# Keycloak theme

Milestone 5. A Keycloakify project (React + TypeScript) that builds a theme JAR for the login pages, OTP setup, recovery codes, passkey registration, update password, error pages, and the account console. The JAR is copied into `providers/` by `keycloak/Dockerfile`.

Until then Keycloak's default theme is used.
