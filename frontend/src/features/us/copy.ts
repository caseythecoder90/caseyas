// Copy that the Us screen owns: strings that differ between the transcribed
// mock data and the design canvas (the canvas is the visual truth), the desktop
// placeholder line, and the honest (pre-milestone) lines.

/** desktop.md section 8: the `isOther` placeholder line for Notes and Us. */
export const DESKTOP_PLACEHOLDER = 'Desktop layout not in this round — see the mobile prototype.'

/** The one explicit hand-off to the account console (identity-pages.md screen 9), design preview. */
export const ACCOUNT_CONSOLE_ROW = 'Manage in account console'

/** The single honest Security row: Keycloak owns every credential and device. */
export const SECURITY_LIVES_IN_CONSOLE = 'Passkeys, authenticator and devices live in your account console'

/** ISO twin of people.TOGETHER_SINCE ('Jul 12, 2024') for the "days" stat. */
export const TOGETHER_SINCE_ISO = '2024-07-12'

export const DEV_PREVIEW_TITLE = 'Design preview data'
export const DEV_PREVIEW_HINT = "Shows the design's placeholder content on the tabs that are not built yet."
